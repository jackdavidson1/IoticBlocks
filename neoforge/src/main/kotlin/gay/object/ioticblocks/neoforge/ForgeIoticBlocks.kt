package gay.`object`.ioticblocks.neoforge

import net.neoforged.fml.common.Mod

import gay.`object`.ioticblocks.IoticBlocks

@Mod("ioticblocks")
class IoticBlocksNeoForge {
    init {
        // Run our common setup.
        IoticBlocks.init();
    }
}